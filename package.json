{
  "name": "castlevania97",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "A 2D pixel crossover fighting game — TypeScript canvas engine.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
