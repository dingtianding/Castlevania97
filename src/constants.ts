/** Logical play-field dimensions (16:9) and the ground line fighters stand on.
 *  Everything in the simulation reasons in this fixed coordinate space. */
export const GAME_WIDTH = 1024
export const GAME_HEIGHT = 576
/** World y where a fighter's feet rest — the top of the brick ground in
 *  background2.png (tuned against the baked-in figure's footing). */
export const FLOOR_Y = 500
